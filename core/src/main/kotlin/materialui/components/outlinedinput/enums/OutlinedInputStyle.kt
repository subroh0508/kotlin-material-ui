package materialui.components.outlinedinput.enums

@Suppress("EnumEntryName")
enum class OutlinedInputStyle {
    root,
    focused,
    disabled,
    adornedStart,
    adornedEnd,
    error,
    multiline,
    notchedOutline,
    input,
    inputMarginDense,
    inputMultiline,
    inputAdornedStart,
    inputAdornedEnd
}