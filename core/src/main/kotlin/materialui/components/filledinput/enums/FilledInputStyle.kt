package materialui.components.filledinput.enums

@Suppress("EnumEntryName")
enum class FilledInputStyle {
    root,
    underline,
    focused,
    disabled,
    adornedStart,
    adornedEnd,
    error,
    multiline,
    input,
    inputMarginDense,
    inputMultiline,
    inputAdornedStart,
    inputAdornedEnd
}